/**
 * FIREBASE COMPLETELY REMOVED.
 * Authentication is powered directly by Google Identity Services (GIS) / Google OAuth 2.0.
 * See: src/lib/googleAuth.ts
 */
export const isFirebaseRemoved = true;
